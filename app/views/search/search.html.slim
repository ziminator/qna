h1 Search results

.result-search
  ul
    - @data.each do |resource|
      li[class="search-#{resource.class.name.downcase}"]
        p = resource.title if resource.respond_to?(:title)
        p = resource.body if resource.respond_to?(:body)
        p = resource.email if resource.respond_to?(:email)

  = will_paginate @data
